{
  "name": "openbuildos-functions",
  "version": "1.0.0",
  "description": "OpenBuildOS token-exchange Cloud Function (authExchange) — jedno přihlášení napříč samostatnými self-host Firebase backendy.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "check": "npm run build && node checks/validators.mjs",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
