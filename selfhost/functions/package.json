{
  "name": "openbuildos-functions",
  "version": "1.0.0",
  "description": "OpenBuildOS token-exchange Cloud Function (authExchange) — jedno přihlášení napříč samostatnými self-host Firebase backendy.",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test lib/**/*.test.js",
    "check": "npm run build && node checks/validators.mjs && node --test lib/**/*.test.js",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.15",
    "sharp": "^0.34.5",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.3",
    "@types/nodemailer": "^6.4.16",
    "@types/unzipper": "^0.10.11",
    "typescript": "^5.5.4"
  }
}
